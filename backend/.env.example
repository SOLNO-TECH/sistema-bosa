PORT=4000
JWT_SECRET=your_secret_key_here
JWT_EXPIRES_IN=8h
NODE_ENV=development

# Web Push (pantalla de bloqueo / PWA). Opcional: si no se definen, se generan en data/vapid.json al arrancar.
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
# VAPID_SUBJECT=mailto:admin@bosa.mx
