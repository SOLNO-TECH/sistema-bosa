/**
 * Versión visible en la app — cambiar solo este valor al desplegar.
 * Producción anterior de referencia: v2.0.0
 */
export const APP_NAME = 'BOSA Hub';

export const APP_VERSION = '2.1.0';

export const APP_VERSION_LABEL = `v${APP_VERSION}`;
